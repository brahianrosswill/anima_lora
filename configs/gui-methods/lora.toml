network_dim = 32
network_alpha = 32
learning_rate = 2e-5
max_train_epochs = 4
save_every_n_epochs = 2
checkpointing_epochs = 2
use_shuffled_caption_variants = true
cache_llm_adapter_outputs = true
caption_dropout_rate = 0.1
output_name = "anima"
blocks_to_swap = 0
use_cmmd = false
sample_prompts = []
sample_every_n_epochs = 0
sample_at_first = false
sample_decode_inline = "false"
use_repa = true
[[datasets]]
validation_split_num = 0
validation_split = 0.0

[variant]
family = "lora"
label = "LoRA"
description = "Plain classic LoRA — simplest baseline."
order = 10
target_res = [ 896, 1024,]
